# Node.js Blog Server

A simple blog server built using Node.js and the built-in **HTTP** module. It demonstrates basic routing, dynamic HTML rendering, and clean styling without any external dependencies.

## Features
- **Home Page:** Introductory page with navigation links.
- **Posts Page:** Dynamically displays 3–4 blog posts from a hardcoded array.
- **404 Handler:** Returns a styled error page for invalid routes.

## Tech Used
Node.js (HTTP module)
HTML & CSS (inline rendering) 